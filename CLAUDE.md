@AGENTS.md

## GBRAIN

Use `AGENTS.md` as the shared repo operating contract. Before meaningful work, query the `gbrain` MCP server for a narrow relevant context slice and read the strongest returned pages. After meaningful work, capture durable artifacts and decisions under source `claude`. Do not store secrets, raw credentials, login codes, or noisy scratch logs.
